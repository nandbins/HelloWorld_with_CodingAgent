Act as a Senior Product Manager. Write a minimalist, bare-minimum Product Requirement Document (PRD) for a simple C command-line program and output it in a code block as a valid PRD.md file. The program requirements are: 1. It is a "Hello World" style CLI application written in C. 2. It must accept a single name as a command-line argument. 3. It must output a greeting message (e.g., "Hello, [Name]!"). 4. If no argument is provided, it should gracefully handle the error and print a usage instruction. Keep the PRD incredibly simple, using only the following markdown sections: - ## Project Overview - ## Requirements (Functional & Non-Functional) - ## Expected Behavior (with a quick CLI example) - ## Out of Scope
